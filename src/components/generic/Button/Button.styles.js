const commonButton = {
  width: 40,
  height: 20,
  textAlign: 'center',
  position: 'absolute',
  padding: 2,
  fontSize: '0.7rem',
  right: 2,
  top: 2,
};

const styles = {
  actionButton: {
    ...commonButton,
  },
  showPassword: {
    ...commonButton,
  },
};

export default styles;
